from django.db import migrations


class Migration(migrations.Migration):
    """
    Stub migration — this was applied to older DBs but the original file was deleted.
    Kept here so Django's migration history stays consistent.
    """

    dependencies = [
        ("core", "0001_initial"),
    ]

    operations = []
